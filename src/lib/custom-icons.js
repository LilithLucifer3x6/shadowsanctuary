// Re-exports the current, live icon set from the repo root so every consumer
// (including components/Icon.jsx) stays in sync with a single source of truth.
// Do not redefine CUSTOM/ic here — edit ../custom-icons.jsx instead.
export { CUSTOM, ic } from './custom-icons.jsx';
